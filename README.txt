How to compile command line using g++ (create executable)
g++ -std=c++0x Main.cpp Reminder.cpp -o myreminder libquickmail.a -lcurl

Run executable using:
./myreminder

--Note: You need to have cURL installed
        You need to have GNU c++ compiler installed
        
Note: You need to unzip the libquickmail in your local git repo
